# frp-manager-lite Docker/Compose environment example
# Copy to .env and change every CHANGE_* value before production use.

# Docker Compose 发布地址和端口。
# 默认 127.0.0.1：仅本机可访问，前面挂 Nginx/Caddy 做 HTTPS 反代。
# 设 0.0.0.0：直接从外网访问（不建议生产裸跑，仅用于测试或无 Nginx 的场景）。
FML_PUBLISH_BIND=127.0.0.1
FML_PUBLISH_PORT=18081

FML_ADMIN_USER=admin
FML_ADMIN_PASSWORD=CHANGE…WORD
FML_PORT_START=20000
FML_PORT_END=20199
FML_DEFAULT_MAX_PORTS=10

FRP_SERVER_ADDR=frp.example.com
FRP_SERVER_PORT=7000
FRP_AUTH_TOKEN=CHANGE…OKEN

# ======== 软件授权（必须配置） ========
# 部署后必须先激活软件授权才能使用面板。
# 方式 A：远程授权服务器（推荐，配合 license-authority 服务）。
# 程序只在安装/首次部署时鉴权一次；成功后生成本地授权文件，重装时读取文件并校验机器指纹。
FML_LICENSE_SERVER_URL=https://license.example.com
# 本地授权文件保存位置；必须放在持久化目录，避免重装丢失。
FML_LICENSE_FILE=/data/software-license.json
# 方式 B：本地签名验证（密钥需与 license-authority 的 LCA_LICENSE_SECRET 一致）
# FML_SOFTWARE_LICENSE_SECRET=CHANGE…CRET
# FML_LICENSE_AUTHORITY=0     # 设为 1 可在面板后台签发服务器软件授权码

# 一键添加 frps 节点的验证密钥（配合 scripts/add-frps-node.sh 使用）
# FML_SETUP_KEY=***

# Optional Cloudflare R2 full-backup upload
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
*** R2_BUCKET=
# R2_PREFIX=frp-manager-lite/backups
